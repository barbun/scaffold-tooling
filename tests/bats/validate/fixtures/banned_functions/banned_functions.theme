<?php

function banned_functions_preprocess_page(&$vars) {
  shell_exec('ls -l');

  print_r('hello');

  $client = \Drupal::httpClient();

  $httpClient = new GuzzleHttp\Client();

  \Drupal::service('router');
  \Drupal::service('page_cache_kill_switch');
}
